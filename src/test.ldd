void MyFunction()
  (: Do stuff here
END

void Print(message)
  wtf (message);
END 

loop dee doo ("hows life")
  num = 1f + 4.00;
  Print("added");
END

void writeToFile(filename, contents)
  file = File.Open(filename);
  file.Write(contents)
END

i += 4.5
i -= 76f
x = 56

func Gen(data)
  return data.Compile()

myGen = Gen()